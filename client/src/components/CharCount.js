import styled from 'styled-components';

const CharCount = styled.span`
  position: absolute;
  bottom: 8px;
  right: 12px;
  font-size: 11px;
  color: #94a3b8;
  pointer-events: none;
`;

export default CharCount;
